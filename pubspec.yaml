name: camera_app
description: Camera app with timer and quality settings
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  path: ^1.9.0
  permission_handler: ^11.3.0
  video_player: ^2.8.6
  wakelock_plus: ^1.2.1
  audioplayers: ^6.1.0
  file_picker: ^8.1.4
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
